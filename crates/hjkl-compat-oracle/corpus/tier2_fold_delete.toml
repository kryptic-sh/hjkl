# Tier 2 — linewise operators on a CLOSED fold.
#
# Vim semantics: a linewise operator (`dd`/`yy`/`cc`/`>>`/`<<`/`gUU`/`guu`/
# `g~~`/`g??`) applied with the cursor on a CLOSED fold operates on the ENTIRE
# fold, not just the cursor line.
#
# Each case builds a closed 3-line fold over lines 2..4 with `zf2j`, then runs
# the operator. `foldmethod` is pinned to `manual` on both drivers (nvim
# defaults manual, hjkl defaults expr) so `zf` behaves identically. Expected
# buffers captured from `nvim --headless`.

# `dd` deletes all 3 folded lines.
[[cases]]
name = "dd_on_closed_fold_deletes_whole_fold"
initial_buffer = "a\nb\nc\nd\ne\n"
initial_cursor = [1, 0]
keys = "zf2jdd"
foldmethod = "manual"
expected_buffer = "a\ne\n"
expected_mode = "normal"

# `yy` then `p` yanks all 3 folded lines and pastes them below the fold.
[[cases]]
name = "yy_on_closed_fold_yanks_whole_fold"
initial_buffer = "a\nb\nc\nd\ne\n"
initial_cursor = [1, 0]
keys = "zf2jyyp"
foldmethod = "manual"
expected_buffer = "a\nb\nc\nd\nb\nc\nd\ne\n"
expected_mode = "normal"

# `cc` changes all 3 folded lines, replacing them with the typed text.
[[cases]]
name = "cc_on_closed_fold_changes_whole_fold"
initial_buffer = "a\nb\nc\nd\ne\n"
initial_cursor = [1, 0]
keys = "zf2jccX<Esc>"
foldmethod = "manual"
expected_buffer = "a\nX\ne\n"
expected_mode = "normal"

# `>>` indents all 3 folded lines by one shiftwidth.
[[cases]]
name = "indent_on_closed_fold_indents_whole_fold"
initial_buffer = "a\nb\nc\nd\ne\n"
initial_cursor = [1, 0]
keys = "zf2j>>"
foldmethod = "manual"
shiftwidth = 2
expandtab = true
expected_buffer = "a\n  b\n  c\n  d\ne\n"
expected_mode = "normal"

# `<<` dedents all 3 folded lines by one shiftwidth.
[[cases]]
name = "dedent_on_closed_fold_dedents_whole_fold"
initial_buffer = "a\n  b\n  c\n  d\ne\n"
initial_cursor = [1, 0]
keys = "zf2j<<"
foldmethod = "manual"
shiftwidth = 2
expandtab = true
expected_buffer = "a\nb\nc\nd\ne\n"
expected_mode = "normal"

# `gUU` uppercases all 3 folded lines.
[[cases]]
name = "gUU_on_closed_fold_uppercases_whole_fold"
initial_buffer = "a\nb\nc\nd\ne\n"
initial_cursor = [1, 0]
keys = "zf2jgUU"
foldmethod = "manual"
expected_buffer = "a\nB\nC\nD\ne\n"
expected_mode = "normal"

# `g~~` toggles case across all 3 folded lines.
[[cases]]
name = "g_tilde_on_closed_fold_toggles_whole_fold"
initial_buffer = "a\nb\nc\nd\ne\n"
initial_cursor = [1, 0]
keys = "zf2jg~~"
foldmethod = "manual"
expected_buffer = "a\nB\nC\nD\ne\n"
expected_mode = "normal"

# `g??` ROT13s all 3 folded lines.
[[cases]]
name = "g_question_on_closed_fold_rot13s_whole_fold"
initial_buffer = "a\nb\nc\nd\ne\n"
initial_cursor = [1, 0]
keys = "zf2jg??"
foldmethod = "manual"
expected_buffer = "a\no\np\nq\ne\n"
expected_mode = "normal"

# ── Charwise operators on a CLOSED fold ───────────────────────────────────────
#
# A *charwise* operator with the cursor on a closed fold also acts on the whole
# fold (`:h fold`: "a closed fold is included as a whole"). Delete (`x`/`dw`/
# `dW`) becomes linewise — the register carries a trailing newline, like `dd`.
# Yank (`yw`/`yW`) and change (`cw`) consume the fold as a whole but keep a
# CHARWISE register (no trailing newline), matching nvim. A single-line fold
# does not expand (there is nothing beyond the one row). `X` is a backward
# delete and is never promoted. Expected buffers/registers captured from
# `nvim --headless` 0.12.5.

# `x` deletes the whole 2-line fold, linewise register.
[[cases]]
name = "x_on_closed_fold_deletes_whole_fold"
initial_buffer = "abc\ndef\nghi\n"
initial_cursor = [0, 0]
keys = "zfjx"
foldmethod = "manual"
expected_buffer = "ghi\n"
expected_mode = "normal"
expected_register = ['"', "abc\ndef\n"]

# `X` on the fold's start column is a backward delete — never promoted (no-op).
[[cases]]
name = "capital_x_on_closed_fold_start_is_noop"
initial_buffer = "abc\ndef\nghi\n"
initial_cursor = [0, 0]
keys = "zfjX"
foldmethod = "manual"
expected_buffer = "abc\ndef\nghi\n"
expected_mode = "normal"
expected_register = ['"', ""]

# `dw` deletes the whole 2-line fold, linewise register.
[[cases]]
name = "dw_on_closed_fold_deletes_whole_fold"
initial_buffer = "abc\ndef\nghi\n"
initial_cursor = [0, 0]
keys = "zfjdw"
foldmethod = "manual"
expected_buffer = "ghi\n"
expected_mode = "normal"
expected_register = ['"', "abc\ndef\n"]

# `dW` (big-word) behaves the same as `dw` on a fold.
[[cases]]
name = "big_word_dW_on_closed_fold_deletes_whole_fold"
initial_buffer = "abc\ndef\nghi\n"
initial_cursor = [0, 0]
keys = "zfjdW"
foldmethod = "manual"
expected_buffer = "ghi\n"
expected_mode = "normal"
expected_register = ['"', "abc\ndef\n"]

# `yw` yanks the whole fold but as a CHARWISE register (no trailing newline).
[[cases]]
name = "yw_on_closed_fold_yanks_whole_fold_charwise"
initial_buffer = "abc\ndef\nghi\n"
initial_cursor = [0, 0]
keys = "zfjyw"
foldmethod = "manual"
expected_buffer = "abc\ndef\nghi\n"
expected_mode = "normal"
expected_register = ['"', "abc\ndef"]

# `yW` (big-word) behaves the same as `yw` on a fold.
[[cases]]
name = "big_word_yW_on_closed_fold_yanks_whole_fold_charwise"
initial_buffer = "abc\ndef\nghi\n"
initial_cursor = [0, 0]
keys = "zfjyW"
foldmethod = "manual"
expected_buffer = "abc\ndef\nghi\n"
expected_mode = "normal"
expected_register = ['"', "abc\ndef"]

# `gUw` uppercases the whole fold linewise (case ops record no register).
[[cases]]
name = "gUw_on_closed_fold_uppercases_whole_fold"
initial_buffer = "abc\ndef\nghi\n"
initial_cursor = [0, 0]
keys = "zfjgUw"
foldmethod = "manual"
expected_buffer = "ABC\nDEF\nghi\n"
expected_mode = "normal"

# `cw` changes the whole fold but keeps a CHARWISE register (no trailing newline).
[[cases]]
name = "cw_on_closed_fold_changes_whole_fold_charwise_register"
initial_buffer = "abc\ndef\nghi\n"
initial_cursor = [0, 0]
keys = "zfjcw"
foldmethod = "manual"
expected_buffer = "\nghi\n"
expected_mode = "insert"
expected_register = ['"', "abc\ndef"]

# A single-line fold does not expand: `x` stays a one-char delete.
[[cases]]
name = "single_line_fold_x_stays_charwise"
initial_buffer = "abc\n"
initial_cursor = [0, 0]
keys = "zf$x"
foldmethod = "manual"
expected_buffer = "bc\n"
expected_mode = "normal"
expected_register = ['"', "a"]

# At column > 0 of a closed fold, nvim keeps normal charwise semantics. The
# reason is `'foldopen'`, whose default contains `hor`: the `l` that moved to
# column 1 OPENED the fold as it finished, so the `x` / `dw` that follows is
# looking at an open fold. hjkl has no `'foldopen'` yet and leaves the fold
# closed, so it stands column 0 in for "nothing has opened this fold yet"
# (`cursor_left_fold_open`); these two cases are what pins that. Within ONE
# command the fold does stay closed in nvim too — see `3e` below.
[[cases]]
name = "x_at_col1_on_closed_fold_stays_charwise"
initial_buffer = "abc\ndef\nghi\n"
initial_cursor = [0, 0]
keys = "zfjlx"
foldmethod = "manual"
expected_buffer = "ac\ndef\nghi\n"
expected_cursor = [0, 1]
expected_mode = "normal"
expected_register = ['"', "b"]

[[cases]]
name = "dw_at_col1_on_closed_fold_stays_charwise"
initial_buffer = "abc\ndef\nghi\n"
initial_cursor = [0, 0]
keys = "zfjldw"
foldmethod = "manual"
expected_buffer = "a\ndef\nghi\n"
expected_mode = "normal"
expected_register = ['"', "bc"]

# ── Word MOTIONS over a closed fold ───────────────────────────────────────────
#
# The whole-fold behaviour above is not a special case bolted onto the
# operator: vim's `fwd_word()` / `end_word()` open every count iteration by
# moving a cursor that is inside a closed fold to the fold's LAST row and
# `coladvance(MAXCOL)`, and `bck_word()` moves it to the fold's FIRST row,
# column 0. `ge` / `gE` have no such jump. What the uncounted `dw` / `yw`
# cases above pin is just that jump plus the ordinary word scan; with a COUNT
# the scan keeps walking past the fold.
#
# On top of that, `do_pending_operator()` includes folded lines completely:
# an endpoint that lands inside a closed fold is pushed out to that fold's far
# edge. That is what makes `dj` / `d3w` stopping part-way into a LATER fold
# take all of it — and it runs after the word-EOL clamp, so a `d3w` whose last
# word ends on the row above a fold pulls back there and never reaches it.
#
# Expected values captured from nvim 0.12.5.

[[cases]]
name = "counted_2dw_on_closed_fold_walks_past_the_fold"
initial_buffer = "abc\ndef\nghi jkl\nmno\n"
initial_cursor = [0, 0]
keys = "zfj2dw"
foldmethod = "manual"
expected_buffer = "jkl\nmno\n"
expected_cursor = [0, 0]
expected_mode = "normal"
expected_register = ['"', "abc\ndef\nghi "]

[[cases]]
name = "counted_3dw_on_closed_fold_walks_past_the_fold"
initial_buffer = "abc\ndef\nghi jkl\nmno\n"
initial_cursor = [0, 0]
keys = "zfj3dw"
foldmethod = "manual"
expected_buffer = "mno\n"
expected_cursor = [0, 0]
expected_mode = "normal"
expected_register = ['"', "abc\ndef\nghi jkl\n"]

[[cases]]
name = "counted_2dW_on_closed_fold_walks_past_the_fold"
initial_buffer = "abc\ndef\nghi jkl\nmno\n"
initial_cursor = [0, 0]
keys = "zfj2dW"
foldmethod = "manual"
expected_buffer = "jkl\nmno\n"
expected_cursor = [0, 0]
expected_mode = "normal"
expected_register = ['"', "abc\ndef\nghi "]

[[cases]]
name = "counted_2yw_on_closed_fold_walks_past_the_fold"
initial_buffer = "abc\ndef\nghi jkl\nmno\n"
initial_cursor = [0, 0]
keys = "zfj2yw"
foldmethod = "manual"
expected_buffer = "abc\ndef\nghi jkl\nmno\n"
expected_cursor = [0, 0]
expected_mode = "normal"
expected_register = ['"', "abc\ndef\nghi "]

[[cases]]
name = "de_on_closed_fold_ends_at_next_word_end"
initial_buffer = "abc\ndef\nghi jkl\nmno\n"
initial_cursor = [0, 0]
keys = "zfjde"
foldmethod = "manual"
expected_buffer = " jkl\nmno\n"
expected_cursor = [0, 0]
expected_mode = "normal"
expected_register = ['"', "abc\ndef\nghi"]

[[cases]]
name = "dE_on_closed_fold_ends_at_next_word_end"
initial_buffer = "abc\ndef\nghi jkl\nmno\n"
initial_cursor = [0, 0]
keys = "zfjdE"
foldmethod = "manual"
expected_buffer = " jkl\nmno\n"
expected_cursor = [0, 0]
expected_mode = "normal"
expected_register = ['"', "abc\ndef\nghi"]

[[cases]]
name = "counted_2de_on_closed_fold_ends_at_second_word_end"
initial_buffer = "abc\ndef\nghi jkl\nmno\n"
initial_cursor = [0, 0]
keys = "zfj2de"
foldmethod = "manual"
expected_buffer = "mno\n"
expected_cursor = [0, 0]
expected_mode = "normal"
expected_register = ['"', "abc\ndef\nghi jkl\n"]

[[cases]]
name = "ye_on_closed_fold_ends_at_next_word_end"
initial_buffer = "abc\ndef\nghi jkl\nmno\n"
initial_cursor = [0, 0]
keys = "zfjye"
foldmethod = "manual"
expected_buffer = "abc\ndef\nghi jkl\nmno\n"
expected_cursor = [0, 0]
expected_mode = "normal"
expected_register = ['"', "abc\ndef\nghi"]

[[cases]]
name = "ce_on_closed_fold_ends_at_next_word_end"
initial_buffer = "abc\ndef\nghi jkl\nmno\n"
initial_cursor = [0, 0]
keys = "zfjce"
foldmethod = "manual"
expected_buffer = " jkl\nmno\n"
expected_cursor = [0, 0]
expected_mode = "insert"
expected_register = ['"', "abc\ndef\nghi"]

[[cases]]
name = "cw_on_closed_fold_stops_at_the_fold_not_the_next_word"
initial_buffer = "abc\ndef\nghi jkl\nmno\n"
initial_cursor = [0, 0]
keys = "zfjcw"
foldmethod = "manual"
expected_buffer = "\nghi jkl\nmno\n"
expected_cursor = [0, 0]
expected_mode = "insert"
expected_register = ['"', "abc\ndef"]

[[cases]]
name = "cW_on_closed_fold_stops_at_the_fold_not_the_next_word"
initial_buffer = "abc\ndef\nghi jkl\nmno\n"
initial_cursor = [0, 0]
keys = "zfjcW"
foldmethod = "manual"
expected_buffer = "\nghi jkl\nmno\n"
expected_cursor = [0, 0]
expected_mode = "insert"
expected_register = ['"', "abc\ndef"]

[[cases]]
name = "counted_2cw_on_closed_fold_ends_at_next_word_end"
initial_buffer = "abc\ndef\nghi jkl\nmno\n"
initial_cursor = [0, 0]
keys = "zfj2cw"
foldmethod = "manual"
expected_buffer = " jkl\nmno\n"
expected_cursor = [0, 0]
expected_mode = "insert"
expected_register = ['"', "abc\ndef\nghi"]

[[cases]]
name = "counted_3cw_on_closed_fold_ends_at_second_word_end"
initial_buffer = "abc\ndef\nghi jkl\nmno\n"
initial_cursor = [0, 0]
keys = "zfj3cw"
foldmethod = "manual"
expected_buffer = "\nmno\n"
expected_cursor = [0, 0]
expected_mode = "insert"
expected_register = ['"', "abc\ndef\nghi jkl"]

[[cases]]
name = "db_on_closed_fold_start_is_noop"
initial_buffer = "abc\ndef\nghi jkl\nmno\n"
initial_cursor = [0, 0]
keys = "zfjdb"
foldmethod = "manual"
expected_buffer = "abc\ndef\nghi jkl\nmno\n"
expected_cursor = [0, 0]
expected_mode = "normal"
expected_register = ['"', ""]

[[cases]]
name = "d0_on_closed_fold_start_is_noop"
initial_buffer = "abc\ndef\nghi jkl\nmno\n"
initial_cursor = [0, 0]
keys = "zfjd0"
foldmethod = "manual"
expected_buffer = "abc\ndef\nghi jkl\nmno\n"
expected_cursor = [0, 0]
expected_mode = "normal"
expected_register = ['"', ""]

[[cases]]
name = "yj_on_closed_fold_covers_fold_and_next_row"
initial_buffer = "abc\ndef\nghi jkl\nmno\n"
initial_cursor = [0, 0]
keys = "zfjyj"
foldmethod = "manual"
expected_buffer = "abc\ndef\nghi jkl\nmno\n"
expected_cursor = [0, 0]
expected_mode = "normal"
expected_register = ['"', "abc\ndef\nghi jkl\n"]

[[cases]]
name = "d3w_ending_in_a_later_fold_takes_all_of_it"
initial_buffer = "one two\nAAA\nBBB\nlast\n"
initial_cursor = [0, 0]
keys = "jzfjkd3w"
foldmethod = "manual"
expected_buffer = "last\n"
expected_cursor = [0, 0]
expected_mode = "normal"
expected_register = ['"', "one two\nAAA\nBBB\n"]

[[cases]]
name = "d4w_crossing_a_later_fold_keeps_walking"
initial_buffer = "one two\nAAA\nBBB\nlast word\n"
initial_cursor = [0, 0]
keys = "jzfjkd4w"
foldmethod = "manual"
expected_buffer = "word\n"
expected_cursor = [0, 0]
expected_mode = "normal"
expected_register = ['"', "one two\nAAA\nBBB\nlast "]

[[cases]]
name = "dj_ending_in_a_later_fold_takes_all_of_it"
initial_buffer = "one two\nAAA\nBBB\nlast\n"
initial_cursor = [0, 0]
keys = "jzfjkdj"
foldmethod = "manual"
expected_buffer = "last\n"
expected_cursor = [0, 0]
expected_mode = "normal"
expected_register = ['"', "one two\nAAA\nBBB\n"]

[[cases]]
name = "d2e_ending_in_a_later_fold_takes_all_of_it"
initial_buffer = "aa\nbb cc\nDD\nee\n"
initial_cursor = [0, 0]
keys = "jzfjkd2e"
foldmethod = "manual"
expected_buffer = "ee\n"
expected_cursor = [0, 0]
expected_mode = "normal"
expected_register = ['"', "aa\nbb cc\nDD\n"]

[[cases]]
name = "d3w_stopping_above_a_fold_never_reaches_it"
initial_buffer = "aa bb\ncc\nDD\nEE\nff\n"
initial_cursor = [0, 0]
keys = "jjzfjkkd3w"
foldmethod = "manual"
expected_buffer = "DD\nEE\nff\n"
expected_cursor = [0, 0]
expected_mode = "normal"
expected_register = ['"', "aa bb\ncc\n"]

[[cases]]
name = "d4w_into_a_fold_takes_all_of_it"
initial_buffer = "aa bb\ncc\nDD\nEE\nff\n"
initial_cursor = [0, 0]
keys = "jjzfjkkd4w"
foldmethod = "manual"
expected_buffer = "ff\n"
expected_cursor = [0, 0]
expected_mode = "normal"
expected_register = ['"', "aa bb\ncc\nDD\nEE\n"]

[[cases]]
name = "d4b_backward_over_a_closed_fold_jumps_to_its_first_row"
initial_buffer = "aaa bbb\nAAA\nBBB\nccc ddd\n"
initial_cursor = [0, 3]
keys = "jzfjj$d4b"
foldmethod = "manual"
expected_buffer = "aaa d\n"
expected_cursor = [0, 4]
expected_mode = "normal"
expected_register = ['"', "bbb\nAAA\nBBB\nccc dd"]

[[cases]]
name = "d4ge_backward_never_jumps_over_a_closed_fold"
initial_buffer = "aaa bbb\nAAA\nBBB\nccc ddd\n"
initial_cursor = [0, 3]
keys = "jzfjj$d4ge"
foldmethod = "manual"
expected_buffer = "aaa bb\n"
expected_cursor = [0, 5]
expected_mode = "normal"
expected_register = ['"', "b\nAAA\nBBB\nccc ddd"]


# Within a SINGLE command the fold is still closed in nvim too: `'foldopen'`
# only fires once the command finishes. So the third iteration of `3e` — which
# starts from a landing inside the fold, at column 1 — still takes the fold
# jump. This is the case a plain "column 0 only" reading of the two cases
# above would get wrong.

[[cases]]
name = "counted_3e_within_one_command_still_sees_the_fold_closed"
initial_buffer = "aa\nbb cc\nDD\nee\n"
initial_cursor = [0, 0]
keys = "jzfjk3e"
foldmethod = "manual"
expected_buffer = "aa\nbb cc\nDD\nee\n"
expected_cursor = [3, 1]
expected_mode = "normal"
expected_register = ['"', ""]

[[cases]]
name = "d3e_within_one_command_still_sees_the_fold_closed"
initial_buffer = "aa\nbb cc\nDD\nee\n"
initial_cursor = [0, 0]
keys = "jzfjkd3e"
foldmethod = "manual"
expected_buffer = ""
expected_cursor = [0, 0]
expected_mode = "normal"
expected_register = ['"', "aa\nbb cc\nDD\nee\n"]

