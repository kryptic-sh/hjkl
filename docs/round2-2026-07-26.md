# Round 2 — DRY / YAGNI / idiomatic / perf (2026-07-26)

Three audits (DRY+YAGNI, Rust idiom, perf) over the whole workspace at v0.37.1.
Full evidence lives in the audit reports; this file tracks the implementation
round. Perf baselines (criterion, ±4% noise): render 41.2/98.6 µs,
insert_char_1MB 23.4 µs, undo cold_jump_back/1024 277 ms, swap write_full/large
d1024 701 µs.

## Fix list

| #   | Item                                                                   | Source | Size | Status |
| --- | ---------------------------------------------------------------------- | ------ | ---- | ------ |
| 1   | `to_options` lossy backfill + `cursorline` default contradiction       | D1     | S    | done   |
| 2   | `display_keys` = `encode_macro` copy — delete                          | D4     | S    | done   |
| 3   | Visual-exit mark math duplicated in step.rs / visual.rs                | D2     | S    | done   |
| 4   | `begin_insert` / `begin_insert_noundo` twins                           | D5     | S    | done   |
| 5   | LSP `json!` deep-clones params per send (measured 1.14 ms/save)        | P3/R2  | S    | done   |
| 6   | Undo `diff()` materializes doc twice per edit (measured 1.65 ms)       | P2     | M    | done   |
| 7   | `search_count` full rescan per cursor move (measured 0.2–1 ms)         | P4     | S    | done   |
| 8   | Picker preview full reparse per frame (measured 4–43 ms)               | P1     | M    | done   |
| 9   | Diff mode O(diff) walk per frame → O(viewport)                         | P8     | S-M  | done   |
| 10  | Fold API lock+clone per query; per-row scans (measured −55% `j`/`k`)   | P5     | M    | done   |
| 11  | Viewport span table copied 3× per recompute                            | P9     | S-M  | done   |
| 12  | Explorer per-frame O(N) passes → dirty_gen caches                      | P7     | M    | done   |
| 13  | Grouped small per-event alloc cleanups                                 | P11    | S    | done   |
| 14  | Style conversion: editor-tui/engine-tui consolidation (Indexed drift)  | D6     | M    | done   |
| 15  | statusline-tui / theme-tui converter dedupe (+2 one-liners)            | D7     | S    | done   |
| 16  | lsp_glue ext table → hjkl-lang registry                                | D8     | M    | done   |
| 17  | `BufferSlot` constructor for ~11 literal sites                         | D3     | M    | todo   |
| 18  | YAGNI deletions: HopState.visual, Scroll\* variants, dead prompt fn,   | Y1-Y4  | S    | todo   |
|     | unused EditorRatatuiExt methods                                        |        |      |        |
| 19  | hjkl-ex test-helper quartet; syntax shim test copies; char-boundary ×3 | D9/D10 | S    | todo   |
| 20  | Idiom smalls: unused_peekable, or_fun_call, assigning_clones,          | R1,R7  | S    | todo   |
|     | format_push_string, &Vec/&PathBuf sigs (+deny lints)                   | R8,R9  |      |        |
|     |                                                                        | R11    |      |        |
| 21  | Idiom mechanical batches: manual_let_else, map_unwrap_or, use_self,    | R5,R6  | M    | todo   |
|     | redundant_pub_crate (+warn lints)                                      | R12,15 |      |        |
| 22  | Invariant unwraps → expect with message (prompt/diff_mode/ex_dispatch) | R14    | S    | todo   |
| 23  | needless_pass_by_value genuine cases (edit.rs, lsp, fs-watch)          | R3     | M    | todo   |
| 24  | redundant_clone reviewed sites (highlighter, comment, tag, rainbow)    | R4     | S    | todo   |

## Deferred (decision or risk)

- **D1 full collapse** of Settings/Options into one type — L, staged for 0.1.0
  per editor.rs:1419; item 1 removes the live hazard only.
- **P6** per-cell span resolve sweep — M-L, layering-order-sensitive; needs the
  sortedness guarantee before attempting.
- **P10** wrap-mode scrolloff O(height²) — wrap is not the default; do with
  P6-class care later.
- **R10** stringly errors → enum in hjkl-app::git — design decision.
- **R13** unnecessary_wraps triage — dispatch-table shapes are deliberate; needs
  per-family review.
- **Y5** hjkl-editor::spec — needs external-consumer confirmation (sqeel/buffr)
  before deletion; doc refresh only.
- **Multicursor `lens` vector** (editor.rs:2423) — O(buffer) per edit but gated
  on unwired multicursor; fix when wired.

## Process

Same as round 1: one item per commit (Opus delegation, reviewed), gates per item
(workspace clippy -D warnings, fmt, nextest incl. affected e2e, oracle
ALL-pass), perf items must show before/after bench numbers, push at phase
boundaries with CI check between.
